# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20   # multipart/form-data parsing (file uploads)

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Validation / settings
pydantic==2.10.4
pydantic-settings==2.7.0

# Background jobs
celery==5.4.0
redis==5.2.1

# NOTE for the candidate:
# The "Process Document" feature needs to read PDFs (page count) and render a
# page to an image. The rest of the codebase uses PyMuPDF for this. Add the
# library you choose here and rebuild the image (see CANDIDATE.md).

PyMuPDF==1.25.5

# Testing
pytest==7.4.4
httpx==0.27.0
